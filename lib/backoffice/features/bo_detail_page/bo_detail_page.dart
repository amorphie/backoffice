import 'package:flutter/material.dart';

class BoDetailPage extends StatelessWidget {
  const BoDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

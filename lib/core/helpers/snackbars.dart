successMessage(String message, [String title = ""]) {
  // ScaffoldMessenger.of(Get.context!).showSnackBar(SnackBar(
  //   content: Row(
  //     children: [
  //       Icon(Icons.check, color: Colors.green[800]),
  //       Expanded(child: LangText(message)),
  //     ],
  //   ),
  // ));
  // Get.snackbar(title, message, icon: Icon(Icons.check, color: Colors.green[800]), snackPosition: SnackPosition.BOTTOM, duration: Duration(seconds: 1), margin: EdgeInsets.only(bottom: 20));
}

errorsMessage(String message, String title) {
//  AppController c = Get.find<AppController>();
//  Get.snackbar(c.translate(title), c.translate(message),
//      icon: Icon(Icons.error_outline, color: Colors.red[800]));
//
  // Get.snackbar(title, message, icon: Icon(Icons.check, color: Colors.green[800]), snackPosition: SnackPosition.BOTTOM, duration: Duration(seconds: 1), margin: EdgeInsets.only(bottom: 20));
}
